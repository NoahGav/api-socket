{
  "name": "api-socket",
  "version": "0.4.2",
  "license": "ISC",
  
  "main": "dist/index.js",
  "types": "dist/index.d.ts",

  "scripts": {
    "build": "rimraf dist && tsc",
    "serve": "rimraf dist && tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.4"
  }
}